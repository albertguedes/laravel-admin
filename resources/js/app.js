import 'tailwindcss';

/**
 * Main JavaScript file for Laravel Admin
 * @author Albert R. C. Guedes
 * @description Main script loaded on all pages. Uses Alpine.js for reactivity.
 */

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();
